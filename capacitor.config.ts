import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'starterkit-ionic-mobile-application',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
